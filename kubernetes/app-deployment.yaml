apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: jdessy
  labels:
    app: app
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: feature-toggle
        - secretRef:
            name: db-secret
        name: app-deployment
        image: registry.dev.afsmtddso.com/jdessy-harborproject/app:971c881-2892fca
        ports:
        - containerPort: 5000
